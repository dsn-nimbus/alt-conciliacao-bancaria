const expect = require('expect')
const {parseOfx, ofxToJSON} = require('./index')

describe('alt-conciliacao-bancaria', () => {
    describe('parseOfx', () => {
        it('deve', () => {
            expect(1).toBe(1)
        })
    })

    describe('ofxToJSON', () => {
        it('deve', () => {
            expect(1).toBe(1)
        })
    })
})